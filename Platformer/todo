Player
    Make run animation faster when sprinting
 
Camera 
    Silent box? Can move a little without camera following
    Refactoring

Enemy AI
    Move til hit object?
    Markers?
    Or set coordinates?
    Go towards player?

Level maker
    Place enemies
    Place collectibles
    Place player start pos

UI 
    Smarter system
    Loads of Refactoring
    Like a "branch system" 
        Always close active UI when pressing escape
        No manual coding

Known issues// (maybe fix)
Camera go too slow( character out of scene)

Fix xmovement smooting with scale

Animation framerate is dependent from framerate (slower the fps, slower the animation)
Really poorly written collision system
    Get's really janky at low fps, starts bumping into stuff etc

